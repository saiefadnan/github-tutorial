#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n;
    int x, y, z;
    cin >> n;

    for (int i = 0; i < n; i++)
    {
        int x;
        cin >> x;

        int count = 0;
        for (int j = 1; j * j <= x; j++)
        {
            if (x % j == 0)
            {
                if (j * j == x)
                {
                    count++;
                }
                else
                {
                    count += 2;
                }
            }
        }
        // It's a change 

        cout << count << endl;
    }

    return 0;
}