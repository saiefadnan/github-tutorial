// add something
#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n;
    cin >> n;

    for (int i = 0; i < n; i++)
    {
        int x;
        cin >> x;
        // 🔥 কেন j * j <= x?
        // 1 × 36
        // 2 × 18
        // 3 × 12
        // 4 × 9
        // 6 × 6   <-- এখানে root pair same
        // খেয়াল করো:

        // 1 পেলে সাথে 36 ও পাওয়া যায়

        // 2 পেলে সাথে 18 ও পাওয়া যায়

        // …

        // মানে, যদি তুমি sqrt(x) পর্যন্ত চেক করো, তাহলে বাকি divisor pair টা নিজে থেকেই পাওয়া যায়।

        // কারণ j <= sqrt(x)
        // অর্থাৎ: যদি j * j > x হয়, তাহলে j আর divisor pair এর প্রথম সংখ্যা হতে পারে না।

        // যদি j * j == x হয় → perfect square → একই divisor দু’বার গণনা করা যাবে না
        // তাই count++

        // যদি j * j < x হয় → দুটি divisor পাওয়া যায়
        // j এবং x/j → তাই count += 2
        int count = 0;
        for (int j = 1; j * j <= x; j++)
        {
            if (x % j == 0)
            {
                if (j * j == x)
                {
                    count++;
                }
                else
                {
                    count += 2;
                }
            }
        }

        cout << count << endl;
    }

    return 0;
}